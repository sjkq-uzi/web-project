{
  "include": ["src/**/*.ts", "src/**/*.d.ts", "src/**/*.tsx", "src/**/*.vue"],
  "exclude": [
    "node_modules",
    "dist",
    "public"
  ],
  "compilerOptions": {
      // ...(原有的配置)
      "baseUrl":"./",   
      "paths": {
        "@/*": ["src/*"]
      }
  },
}
